
import './SkillForm.css'; // Import the CSS file for styling

const UserDashboard = () => {
    return(
        <div>
            <h1>UserDashboard</h1>
        </div>
    )
};

export default UserDashboard;
